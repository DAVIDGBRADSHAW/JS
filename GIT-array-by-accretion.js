/*
let array = [];
for (whatevr limits are appropiate too the application) {
        array.push(the new element);
}

function createArray (n, value){
    let array = [];
    for (let i= 0 ; < n; i++) {
        array.ush(value);
    }
    return array;
}

*/