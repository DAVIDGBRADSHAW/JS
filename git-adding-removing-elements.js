/*
push(Element, ... add one or move elements to END of the array)
pop(removes and returns the last element in the array)

shift(removes and returns the first element in the array)

unshift(element, ... -adds one or more elements to the front of the array)

slice(start, finish)(returns a subarray beginning at start and ending just before finish)

splice( index, count,...) removes count elements starting at index and optionally add new one.

*/