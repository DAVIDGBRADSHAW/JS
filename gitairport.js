const AIRPORT_CODES = {
    'atl': 'AtlANIC, GA, USA',
    'dub': 'DUBLIN, RoI'
}

let codes = AIRPORT_CODES;

let newcodew = new Map();

newcode.set('CRK', 'CORK, IRELAND');
newcode.set('SHA', 'CLARE, SHANNON, IRELAND');

let london_Station = newcode.get('GLN');

for (let[key, value] of newcode) {
    console.log("key; " + key + "value:" + value);
}
